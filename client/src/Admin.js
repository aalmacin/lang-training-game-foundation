import {Logout} from "./Logout";

export const Admin = () => {
    return (
        <div className="flex flex-col items-center">
            <h1>Admin</h1>
            <Logout />
        </div>
    );
}